name: OCI ARM Instance Forge

on:
  workflow_dispatch:
  schedule:
    - cron: '*/10 * * * *'

jobs:
  run-oci-script:
    runs-on: ubuntu-latest
    steps:
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install OCI SDK
        run: |
          pip install oci

      - name: Run Auto Launch Script
        run: |
          cat << 'EOF' > launch.py
          import os
          import sys
          import oci

          # 写入私钥
          private_key = os.environ.get("OCI_PRIVATE_KEY", "").strip()
          with open("/tmp/oci_api_key.pem", "w") as f:
              f.write(private_key)

          config = {
              "user": os.environ.get("OCI_USER_ID", "").strip(),
              "key_file": "/tmp/oci_api_key.pem",
              "fingerprint": os.environ.get("OCI_KEY_FINGERPRINT", "").strip(),
              "tenancy": os.environ.get("OCI_TENANCY_ID", "").strip(),
              "region": os.environ.get("OCI_REGION", "").strip()
          }

          compute_client = oci.core.ComputeClient(config)

          # 提取环境变量
          shape = "VM.Standard.A1.Flex"
          ocpus = float(os.environ.get("OCI_OCPUS", "4"))
          memory_in_gbs = float(os.environ.get("OCI_MEMORY_IN_GBS", "24"))
          
          ad = os.environ.get("OCI_AVAILABILITY_DOMAIN", "").strip()
          compartment_id = os.environ.get("OCI_TENANCY_ID", "").strip()
          subnet_id = os.environ.get("OCI_SUBNET_ID", "").strip()
          boot_volume_id = os.environ.get("OCI_BOOT_VOLUME_ID", "").strip()

          # 1. 检查是否存在正在运行的 ARM 实例
          try:
              print("正在检查是否已存在实例...")
              instances = compute_client.list_instances(
                  compartment_id=compartment_id,
                  lifecycle_state="RUNNING"
              ).data
              arm_instances = [i for i in instances if i.shape == shape]
              if arm_instances:
                  print(f" 检测到已存在运行中的 ARM 实例 ({arm_instances[0].display_name})，停止继续开机！")
                  sys.exit(0)
          except Exception as e:
              print(f" 实例状态检查跳过: {str(e)}")

          # 2. 构造 VNIC Details
          vnic = oci.core.models.CreateVnicDetails(
              subnet_id=subnet_id,
              assign_public_ip=True
          )

          # 3. 构造 Source Details（必须带上 source_type="bootVolume"）
          src = oci.core.models.InstanceSourceViaBootVolumeDetails(
              source_type="bootVolume",
              boot_volume_id=boot_volume_id
          )

          # 4. 构造 Shape Config
          cfg = oci.core.models.LaunchInstanceShapeConfigDetails(
              ocpus=ocpus,
              memory_in_gbs=memory_in_gbs
          )

          # 5. 组装 LaunchDetails
          launch_details = oci.core.models.LaunchInstanceDetails(
              compartment_id=compartment_id,
              availability_domain=ad,
              shape=shape,
              shape_config=cfg,
              create_vnic_details=vnic,
              source_details=src
          )

          # 6. 发送请求
          try:
              print("Sending request to Oracle Cloud API...")
              response = compute_client.launch_instance(launch_details)
              print(" SUCCESS! Instance created successfully!")
              print(f"Instance OCID: {response.data.id}")
          except oci.exceptions.ServiceError as e:
              if e.status == 500 or "Out of host capacity" in str(e.message):
                  print(" Out of host capacity (Capacity full, will retry next run).")
              elif e.status == 429 or "TooManyRequests" in str(e.code):
                  print(" Too Many Requests (API Rate Limit hit, will retry next run).")
              else:
                  print(f" OCI Service Error [{e.status}]: {e.code} - {e.message}")
          except Exception as e:
              print(f" Unexpected Error: {str(e)}")
          EOF

          python launch.py
        env:
          OCI_REGION: ${{ secrets.OCI_REGION }}
          OCI_USER_ID: ${{ secrets.OCI_USER_OCID }}
          OCI_TENANCY_ID: ${{ secrets.OCI_TENANCY_OCID }}
          OCI_KEY_FINGERPRINT: ${{ secrets.OCI_KEY_FINGERPRINT }}
          OCI_PRIVATE_KEY: ${{ secrets.OCI_PRIVATE_KEY }}
          OCI_SUBNET_ID: ${{ secrets.OCI_SUBNET_ID }}
          OCI_BOOT_VOLUME_ID: ${{ secrets.OCI_BOOT_VOLUME_ID }}
          OCI_AVAILABILITY_DOMAIN: ${{ secrets.OCI_AVAILABILITY_DOMAIN }}
          OCI_OCPUS: ${{ secrets.OCI_OCPUS }}
          OCI_MEMORY_IN_GBS: ${{ secrets.OCI_MEMORY_IN_GBS }}
